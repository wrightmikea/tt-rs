# commands

Command pattern implementation for encapsulating operations.

## Crates

- **tt-rs-commands**: `Command` trait, `CommandDispatcher`, `MoveCommand`, `RemoveCommand`

## Dependency Level

Depends on: core, state

## Design Pattern

Gang of Four Command pattern - decouples invoker from receiver.
